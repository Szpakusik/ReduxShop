const TEST_ACTION = 'TEST_ACTION';

const initState = {



}

const someReducer = (state = initState, action) => {
  // switch (action.type) {
  //   case TEST_ACTION:
  //     return state;
  //   default:
  //     return state;
  // }
  return state
};

export default someReducer;
